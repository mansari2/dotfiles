diff -ruN --ex clorch/constants.py /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/constants.py
--- clorch/constants.py	2026-02-27 08:59:30
+++ /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/constants.py	2026-02-27 08:28:37
@@ -17,11 +17,11 @@
 
 # Display mapping: symbol, label, hex color
 STATUS_DISPLAY: dict[AgentStatus, tuple[str, str, str]] = {
-    AgentStatus.WORKING:            (">>>", "WORK", "#A3BE8C"),
-    AgentStatus.IDLE:               ("---", "IDLE", "#616E88"),
-    AgentStatus.WAITING_PERMISSION: ("[!]", "PERM", "#BF616A"),
-    AgentStatus.WAITING_ANSWER:     ("[?]", "WAIT", "#EBCB8B"),
-    AgentStatus.ERROR:              ("[X]", "ERR",  "#B48EAD"),
+    AgentStatus.WORKING:            (">>>", "WORK", "#a6e3a1"),
+    AgentStatus.IDLE:               ("---", "IDLE", "#9399b2"),
+    AgentStatus.WAITING_PERMISSION: ("[!]", "PERM", "#f38ba8"),
+    AgentStatus.WAITING_ANSWER:     ("[?]", "WAIT", "#f9e2af"),
+    AgentStatus.ERROR:              ("[X]", "ERR",  "#f5c2e7"),
 }
 
 # Statuses that need user attention (used by "jump to next red")
@@ -31,20 +31,20 @@
     AgentStatus.ERROR,
 })
 
-# Nord Aurora theme palette
+# Catppuccin Mocha theme palette
 THEME = {
-    "bg":      "#2E3440",
-    "green":   "#A3BE8C",
-    "cyan":    "#88C0D0",
-    "pink":    "#B48EAD",
-    "red":     "#BF616A",
-    "yellow":  "#EBCB8B",
-    "grey":    "#616E88",
-    "fg":      "#D8DEE9",
-    "dim":     "#4C566A",
-    "border":  "#3B4252",
-    "accent":  "#434C5E",
-    "bright":  "#ECEFF4",
+    "bg":      "#1e1e2e",
+    "green":   "#a6e3a1",
+    "cyan":    "#94e2d5",
+    "pink":    "#f5c2e7",
+    "red":     "#f38ba8",
+    "yellow":  "#f9e2af",
+    "grey":    "#9399b2",
+    "fg":      "#cdd6f4",
+    "dim":     "#7f849c",
+    "border":  "#585b70",
+    "accent":  "#313244",
+    "bright":  "#cdd6f4",
 }
 
 # Convenience aliases — single source of truth for TUI and tmux colours.
diff -ruN --ex clorch/hooks/event_handler.sh /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/hooks/event_handler.sh
--- clorch/hooks/event_handler.sh	2026-02-27 08:59:30
+++ /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/hooks/event_handler.sh	2026-02-27 07:48:20
@@ -75,15 +75,6 @@
 # PPID is the Claude Code process — stored so the TUI can detect dead sessions.
 # TMUX is set when running inside a tmux pane — detect the window name.
 CWD_FROM_INPUT="$(echo "$INPUT_JSON" | jq -r '.cwd // empty')"
-# Detect VS Code when TERM_PROGRAM is not set (extension runs outside a terminal).
-# Check env vars first, then fall back to parent process path (.vscode/extensions/).
-if [[ -z "${TERM_PROGRAM:-}" ]]; then
-    if [[ -n "${VSCODE_PID:-}" || -n "${VSCODE_IPC_HOOK_CLI:-}" ]]; then
-        TERM_PROGRAM="vscode"
-    elif ps -p "$PPID" -o command= 2>/dev/null | grep -q '\.vscode/extensions/'; then
-        TERM_PROGRAM="vscode"
-    fi
-fi
 TMUX_WINDOW=""
 TMUX_PANE=""
 # Detect tmux window AND pane ONLY if Claude Code's tty is actually inside a tmux pane.
diff -ruN --ex clorch/notifications/macos.py /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/notifications/macos.py
--- clorch/notifications/macos.py	2026-02-27 08:59:30
+++ /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/notifications/macos.py	2026-02-27 09:21:21
@@ -1,18 +1,42 @@
-"""macOS native notifications via osascript."""
+"""macOS native notifications via terminal-notifier (with iTerm activation)."""
 from __future__ import annotations
 
+import os
+import shutil
 import subprocess
 import sys
 
 
-def notify(title: str, message: str, sound: str = "Ping") -> bool:
+def notify(title: str, message: str, sound: str = "Ping", activate_app: str = "com.googlecode.iterm2") -> bool:
     """Send a macOS notification.
 
+    Uses terminal-notifier if available (clicking "Show" activates iTerm),
+    falls back to osascript.
+
     Returns True if successful, False if not on macOS or failed.
     """
     if sys.platform != "darwin":
         return False
 
+    # Prefer terminal-notifier — clicking the notification activates iTerm
+    # Use full path since hooks/subprocesses may not inherit user's PATH
+    tn = shutil.which("terminal-notifier") or "/opt/homebrew/bin/terminal-notifier"
+    if os.path.isfile(tn):
+        try:
+            cmd = [
+                tn,
+                "-title", title,
+                "-message", message,
+                "-sound", sound,
+                "-activate", activate_app,
+                "-group", "clorch",
+            ]
+            subprocess.run(cmd, capture_output=True, check=True, timeout=5)
+            return True
+        except (subprocess.CalledProcessError, subprocess.TimeoutExpired, FileNotFoundError):
+            pass
+
+    # Fallback to osascript (clicking "Show" opens Script Editor unfortunately)
     script = (
         f'display notification "{_escape(message)}" '
         f'with title "{_escape(title)}" '
@@ -46,7 +70,6 @@
 
 def notify_question(project: str, message: str) -> bool:
     """Notify about a question/elicitation."""
-    # Truncate message for notification
     short = message[:100] + "..." if len(message) > 100 else message
     return notify(
         title=f"Clorch — {project}",
diff -ruN --ex clorch/terminal/detect.py /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/terminal/detect.py
--- clorch/terminal/detect.py	2026-02-27 08:59:30
+++ /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/terminal/detect.py	2026-02-27 07:48:20
@@ -11,9 +11,7 @@
     "iTerm.app": "iTerm",
     "Apple_Terminal": "Terminal.app",
     "ghostty": "Ghostty",
-    "WarpTerminal": "Warp",
     "tmux": "tmux",
-    "vscode": "VS Code",
 }
 
 
diff -ruN --ex clorch/tui/app.py /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/tui/app.py
--- clorch/tui/app.py	2026-02-27 08:59:30
+++ /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/tui/app.py	2026-02-27 09:39:53
@@ -170,6 +170,7 @@
         Binding("k", "cursor_up", "Up", show=False),
         Binding("right", "jump_to_agent", "Jump", show=False),
         Binding("enter", "jump_to_agent", "Jump", show=False),
+        Binding("t", "jump_to_agent", "Jump", show=False),
     ]
 
     def __init__(self) -> None:
@@ -685,15 +686,17 @@
     def _jump_to_session(self, agent: AgentState) -> None:
         """Jump to the terminal running the agent's Claude process.
 
-        Two paths depending on how the agent is running:
+        Three paths depending on how the agent is running:
         - **tmux**: ``select-window`` + ``select-pane`` (CC mode
           auto-switches the terminal tab), then bring terminal to front.
         - **plain terminal**: PID → tty → terminal tab activation.
+        - **fallback for tmux**: switch to existing tmux session if one
+          matches the agent's project name.
 
         Dead processes are cleaned up inline so the user never lands
-        on a stale tab.  Agents in unreachable terminals are blocked
-        with a warning.
+        on a stale tab.
         """
+        import subprocess
         from clorch.tmux.navigator import (
             jump_to_tab, select_tmux_pane, bring_terminal_to_front,
             pid_alive,
@@ -701,15 +704,6 @@
 
         name = agent.project_name or agent.session_id[:12]
 
-        # Reachability check — can't jump to agent in a different terminal
-        table = self.query_one("#session-list", SessionList)
-        if not table.is_agent_reachable(agent):
-            from clorch.tui.widgets.session_list import _agent_terminal_group
-            remote = _agent_terminal_group(agent)
-            local = table._local_terminal
-            self.notify(f"Cannot reach {name} from {local} (agent in {remote})", severity="warning")
-            return
-
         # Dead process check — remove stale state file immediately
         if agent.pid and not pid_alive(agent.pid):
             state_file = self._manager._state_dir / f"{agent.session_id}.json"
@@ -729,7 +723,67 @@
             self.notify(f"Jumped to {name}")
             return
 
-        if not agent.pid:
+        # Fallback: try to jump to an existing tmux session for this project
+        if agent.cwd:
+            import os
+            from clorch.terminal import get_backend
+            session_name = os.path.basename(agent.cwd).replace(".", "_").replace("/", "_")
+            result = subprocess.run(
+                ["tmux", "has-session", "-t", session_name],
+                capture_output=True,
+            )
+            if result.returncode == 0:
+                # Step 1: Find and activate the iTerm tab running tmux
+                backend = get_backend()
+                tab_found = backend.activate_by_name("tmux")
+                if not tab_found:
+                    tab_found = backend.activate_by_name(session_name)
+
+                if tab_found:
+                    # Step 2: Switch tmux to the target session
+                    subprocess.run(
+                        ["tmux", "switch-client", "-t", session_name],
+                        capture_output=True,
+                    )
+                    self.notify(f"Jumped to tmux session: {session_name}")
+                else:
+                    # No iTerm tab has tmux — open a new iTerm window and attach
+                    attach_script = f'''
+                        tell application "iTerm2"
+                            create window with default profile command "/opt/homebrew/bin/tmux attach-session -t {session_name}"
+                            activate
+                        end tell
+                    '''
+                    subprocess.run(
+                        ["osascript", "-e", attach_script],
+                        capture_output=True,
+                        timeout=5,
+                    )
+                    self.notify(f"Opened {session_name} in new iTerm window")
+                return
+
+        # Fallback for iTerm agents: try multiple strategies to find the tab
+        from clorch.tui.widgets.session_list import _agent_terminal_group
+        from clorch.terminal import get_backend
+        terminal = _agent_terminal_group(agent)
+        if terminal and terminal != "tmux":
+            backend = get_backend()
+            # Try multiple search terms to find the right iTerm tab
+            search_terms = [name]
+            if agent.cwd:
+                import os
+                search_terms.append(os.path.basename(agent.cwd))
+            search_terms.extend(["claude", "Claude"])
+
+            for term in search_terms:
+                if backend.activate_by_name(term):
+                    self.notify(f"Jumped to {name}")
+                    return
+
+            # Last resort: just bring iTerm to front
+            backend.bring_to_front()
+            self.notify(f"Opened iTerm — {name} is in one of the tabs", severity="information")
+        elif not agent.pid:
             self.notify(f"{name}: no PID — restart session", severity="warning")
         else:
             self.notify(f"No tab found for {name}", severity="warning")
@@ -957,11 +1011,13 @@
             self.query_one("#detail-panel", AgentDetail).show_agent(None)
 
     def action_jump_to_agent(self) -> None:
-        """Jump to the selected agent's tmux window."""
+        """Jump to the selected agent's terminal. Exits clorch so the popup closes."""
         table = self.query_one("#session-list", SessionList)
         agent = table.get_selected_agent()
         if agent:
             self._jump_to_session(agent)
+            # Exit clorch so the tmux popup closes and the jump is visible
+            self.exit()
 
     def action_refresh(self) -> None:
         self._poll_state()
diff -ruN --ex clorch/tui/app.tcss /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/tui/app.tcss
--- clorch/tui/app.tcss	2026-02-27 08:59:30
+++ /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/tui/app.tcss	2026-02-27 08:31:11
@@ -1,7 +1,7 @@
-/* -- Nord Aurora theme — lazydocker-style bordered panels -- */
+/* -- Catppuccin Mocha theme — lazydocker-style bordered panels -- */
 
 Screen {
-    background: #2E3440;
+    background: #1e1e2e;
 }
 
 /* -- Header: docked top, no border -- */
@@ -19,6 +19,7 @@
     height: 1;
     padding: 0 1;
     dock: bottom;
+    color: #9399b2;
 }
 
 /* -- Main split: list left, right panel right -- */
@@ -32,14 +33,14 @@
 #list-pane {
     width: 2fr;
     height: 1fr;
-    border: round #4C566A;
-    border-title-color: #88C0D0;
+    border: round #585b70;
+    border-title-color: #89b4fa;
     border-title-style: bold;
     padding: 0;
 }
 
 #list-pane:focus-within {
-    border: round #88C0D0;
+    border: round #89b4fa;
 }
 
 /* -- Column header inside list pane -- */
@@ -55,11 +56,11 @@
     height: 1fr;
     min-height: 4;
     scrollbar-size: 1 1;
-    background: #2E3440;
+    background: #1e1e2e;
 }
 
 #session-list > ListItem.--highlight {
-    background: #4C566A;
+    background: #45475a;
 }
 
 /* -- Right panel: vertical stack of sub-panels -- */
@@ -82,8 +83,8 @@
 #settings-panel {
     height: auto;
     max-height: 6;
-    border: round #4C566A;
-    border-title-color: #88C0D0;
+    border: round #585b70;
+    border-title-color: #89b4fa;
     border-title-style: bold;
     padding: 0 1;
 }
@@ -91,8 +92,8 @@
 #detail-panel {
     height: auto;
     max-height: 8;
-    border: round #4C566A;
-    border-title-color: #88C0D0;
+    border: round #585b70;
+    border-title-color: #89b4fa;
     border-title-style: bold;
     padding: 0 1;
 }
@@ -100,19 +101,19 @@
 #telemetry-panel {
     height: auto;
     max-height: 12;
-    border: round #4C566A;
-    border-title-color: #88C0D0;
+    border: round #585b70;
+    border-title-color: #89b4fa;
     border-title-style: bold;
     padding: 0 1;
 }
 
 #event-log-panel {
     height: 1fr;
-    border: round #4C566A;
-    border-title-color: #88C0D0;
+    border: round #585b70;
+    border-title-color: #89b4fa;
     border-title-style: bold;
     padding: 0 1;
-    background: #2E3440;
+    background: #1e1e2e;
     scrollbar-size-horizontal: 0;
     scrollbar-size-vertical: 0;
 }
diff -ruN --ex clorch/tui/widgets/agent_detail.py /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/tui/widgets/agent_detail.py
--- clorch/tui/widgets/agent_detail.py	2026-02-27 08:59:30
+++ /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/tui/widgets/agent_detail.py	2026-02-27 08:30:48
@@ -56,9 +56,9 @@
 
         # Header: [!] PERM  project_name  ToolName
         text.append("[!] PERM", style=f"bold {RED}")
-        text.append("  ", style="dim")
+        text.append("  ", style=f"{GREY}")
         text.append(agent.project_name or agent.session_id[:12], style="bold white")
-        text.append("  ", style="dim")
+        text.append("  ", style=f"{GREY}")
         text.append(agent.last_tool or "?", style=f"bold {YELLOW}")
         text.append("\n")
 
@@ -79,11 +79,11 @@
 
         # Footer: action hints
         text.append("[y]", style=f"bold reverse {GREEN}")
-        text.append(" APPROVE  ", style="dim")
+        text.append(" APPROVE  ", style=f"{GREY}")
         text.append("[n]", style=f"bold reverse {RED}")
-        text.append(" DENY  ", style="dim")
+        text.append(" DENY  ", style=f"{GREY}")
         text.append("[->]", style=f"bold {CYAN}")
-        text.append(" jump", style="dim")
+        text.append(" jump", style=f"{GREY}")
 
         self.update(text)
 
@@ -98,10 +98,10 @@
         text.append("\n")
 
         # Status + Uptime
-        text.append(f"{'Status':<{_LABEL_W}s}", style=f"dim {GREY}")
+        text.append(f"{'Status':<{_LABEL_W}s}", style=f"{GREY}")
         text.append(f"{symbol} {label}", style=f"bold {color}")
-        text.append("    ", style="dim")
-        text.append(f"{'Uptime':<8s}", style=f"dim {GREY}")
+        text.append("    ", style=f"{GREY}")
+        text.append(f"{'Uptime':<8s}", style=f"{GREY}")
         text.append(f"{agent.uptime}", style="white")
         text.append("\n")
 
@@ -114,13 +114,13 @@
                 parts = path.split("/", 2)
                 if len(parts) >= 3:
                     path = "~/" + parts[2]
-            text.append(f"{'Path':<{_LABEL_W}s}", style=f"dim {GREY}")
+            text.append(f"{'Path':<{_LABEL_W}s}", style=f"{GREY}")
             text.append(f"{path}", style=GREEN)
             text.append("\n")
 
         # Git info
         if agent.git_branch:
-            text.append(f"{'Git':<{_LABEL_W}s}", style=f"dim {GREY}")
+            text.append(f"{'Git':<{_LABEL_W}s}", style=f"{GREY}")
             text.append(agent.git_branch, style=f"bold {CYAN}")
             if agent.git_dirty_count > 0:
                 text.append(f" ({agent.git_dirty_count} dirty)", style=f"bold {YELLOW}")
@@ -133,7 +133,7 @@
             try:
                 last_t = datetime.fromisoformat(agent.last_event_time.replace("Z", "+00:00"))
                 age_s = (datetime.now(timezone.utc) - last_t).total_seconds()
-                text.append(f"{'Last event':<{_LABEL_W}s}", style=f"dim {GREY}")
+                text.append(f"{'Last event':<{_LABEL_W}s}", style=f"{GREY}")
                 if age_s > 120:
                     mins = int(age_s) // 60
                     secs = int(age_s) % 60
@@ -147,46 +147,46 @@
                 pass
 
         # Model + Last tool
-        text.append(f"{'Model':<{_LABEL_W}s}", style=f"dim {GREY}")
+        text.append(f"{'Model':<{_LABEL_W}s}", style=f"{GREY}")
         text.append(f"{agent.model or '-'}", style=CYAN)
         if agent.last_tool:
-            text.append("    ", style="dim")
-            text.append(f"{'Last tool':<10s}", style=f"dim {GREY}")
+            text.append("    ", style=f"{GREY}")
+            text.append(f"{'Last tool':<10s}", style=f"{GREY}")
             text.append(f"{agent.last_tool}", style="white")
         text.append("\n")
 
         # Counts line: Tools, Errors, Subagents, Compacts, Tasks
-        text.append(f"{'Tools':<{_LABEL_W}s}", style=f"dim {GREY}")
+        text.append(f"{'Tools':<{_LABEL_W}s}", style=f"{GREY}")
         text.append(f"{agent.tool_count}", style="white")
-        text.append("    ", style="dim")
-        text.append("Errors ", style=f"dim {GREY}")
+        text.append("    ", style=f"{GREY}")
+        text.append("Errors ", style=f"{GREY}")
         text.append(
             f"{agent.error_count}",
             style=f"bold {PINK}" if agent.error_count else f"dim {GREY}",
         )
         if agent.subagent_count:
-            text.append("    ", style="dim")
-            text.append("Subs ", style=f"dim {GREY}")
+            text.append("    ", style=f"{GREY}")
+            text.append("Subs ", style=f"{GREY}")
             text.append(f"{agent.subagent_count}", style=CYAN)
         if agent.compact_count:
-            text.append("    ", style="dim")
-            text.append("Compacts ", style=f"dim {GREY}")
+            text.append("    ", style=f"{GREY}")
+            text.append("Compacts ", style=f"{GREY}")
             text.append(f"{agent.compact_count}", style=PINK)
         if agent.task_completed_count:
-            text.append("    ", style="dim")
-            text.append("Tasks ", style=f"dim {GREY}")
+            text.append("    ", style=f"{GREY}")
+            text.append("Tasks ", style=f"{GREY}")
             text.append(f"{agent.task_completed_count}", style=GREEN)
         text.append("\n")
 
         # Extended sparkline (use all available history, up to 20 chars)
-        text.append(f"{'Activity':<{_LABEL_W}s}", style=f"dim {GREY}")
+        text.append(f"{'Activity':<{_LABEL_W}s}", style=f"{GREY}")
         sparkline = self._render_extended_sparkline(agent.activity_history)
         text.append_text(sparkline)
         text.append("\n")
 
         # Notification message
         if agent.notification_message:
-            text.append(f"{'Msg':<{_LABEL_W}s}", style=f"dim {GREY}")
+            text.append(f"{'Msg':<{_LABEL_W}s}", style=f"{GREY}")
             msg = agent.notification_message
             if len(msg) > 80:
                 msg = msg[:78] + ".."
@@ -200,7 +200,7 @@
         # Use up to 20 most recent data points
         recent = history[-20:] if len(history) >= 20 else history
         if not recent or max(recent) == 0:
-            return Text("\u2581" * min(len(recent), 20), style=f"dim {GREY}")
+            return Text("\u2581" * min(len(recent), 20), style=f"{GREY}")
         max_val = max(recent)
         chars = []
         for v in recent:
diff -ruN --ex clorch/tui/widgets/context_footer.py /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/tui/widgets/context_footer.py
--- clorch/tui/widgets/context_footer.py	2026-02-27 08:59:30
+++ /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/tui/widgets/context_footer.py	2026-02-27 09:29:53
@@ -19,7 +19,7 @@
     DEFAULT_CSS = """
     ContextFooter {
         height: 1;
-        color: #666666;
+        color: #9399b2;
         padding: 0 1;
     }
     """
@@ -28,6 +28,10 @@
         super().__init__("", **kwargs)
         self._mode = "default"
 
+    def on_mount(self) -> None:
+        """Render footer on mount so it's visible immediately."""
+        self._render_footer()
+
     def set_mode(self, mode: str) -> None:
         """Switch footer mode: 'default', 'actions', or 'approval'."""
         if mode == self._mode:
@@ -58,7 +62,7 @@
             text.append("select action ", style=f"{GREY}")
             text.append("[Y]", style=f"bold {GREEN}")
             text.append("approve all ", style=f"{GREY}")
-            text.append("│ ", style=f"dim {GREY}")
+            text.append("│ ", style=f"{GREY}")
             text.append("[j/k]", style=f"bold {CYAN}")
             text.append("nav ", style=f"{GREY}")
             text.append("[->]", style=f"bold {CYAN}")
diff -ruN --ex clorch/tui/widgets/header_bar.py /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/tui/widgets/header_bar.py
--- clorch/tui/widgets/header_bar.py	2026-02-27 08:59:30
+++ /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/tui/widgets/header_bar.py	2026-02-27 08:29:07
@@ -74,43 +74,43 @@
                 text.append(" \u26a0 YOLO ", style=f"bold white on {RED}")
             else:
                 text.append(" \u26a0 YOLO ", style=f"bold {RED}")
-        text.append(" \u2500\u2500\u2500 ", style=f"dim {GREY}")
+        text.append(" \u2500\u2500\u2500 ", style=f"{GREY}")
 
         # tmux session name (if available)
         if self._tmux_session:
             text.append(self._tmux_session, style=f"{CYAN}")
-            text.append(" \u2500\u2500\u2500 ", style=f"dim {GREY}")
+            text.append(" \u2500\u2500\u2500 ", style=f"{GREY}")
 
         # Status counts — full words
         if summary.working > 0:
             spinner = BRAILLE_SPINNER[self._anim_frame % len(BRAILLE_SPINNER)]
             text.append(f"{spinner} ", style=f"bold {GREEN}")
-        text.append("Working: ", style="dim")
+        text.append("Working: ", style=f"{GREY}")
         text.append(str(summary.working), style=f"bold {GREEN}")
-        text.append(" \u2502 ", style=f"dim {GREY}")
+        text.append(" \u2502 ", style=f"{GREY}")
 
-        text.append("Idle: ", style="dim")
+        text.append("Idle: ", style=f"{GREY}")
         text.append(str(summary.idle), style=f"{GREY}")
-        text.append(" \u2502 ", style=f"dim {GREY}")
+        text.append(" \u2502 ", style=f"{GREY}")
 
-        text.append("Perm: ", style="dim")
+        text.append("Perm: ", style=f"{GREY}")
         text.append(str(summary.waiting_permission), style=f"bold {RED}")
-        text.append(" \u2502 ", style=f"dim {GREY}")
+        text.append(" \u2502 ", style=f"{GREY}")
 
-        text.append("Ask: ", style="dim")
+        text.append("Ask: ", style=f"{GREY}")
         text.append(str(summary.waiting_answer), style=f"bold {YELLOW}")
-        text.append(" \u2502 ", style=f"dim {GREY}")
+        text.append(" \u2502 ", style=f"{GREY}")
 
-        text.append("Errors: ", style="dim")
+        text.append("Errors: ", style=f"{GREY}")
         text.append(str(summary.error), style=f"bold {PINK}")
 
         # tools/min rate
         if self._tools_per_min >= 1.0:
-            text.append(" \u2502 ", style=f"dim {GREY}")
-            text.append(f"{int(self._tools_per_min)} t/m", style="dim")
+            text.append(" \u2502 ", style=f"{GREY}")
+            text.append(f"{int(self._tools_per_min)} t/m", style=f"{GREY}")
 
         # Agent total
-        text.append(" \u2500\u2500\u2500 ", style=f"dim {GREY}")
+        text.append(" \u2500\u2500\u2500 ", style=f"{GREY}")
         text.append(f"{summary.total} agents", style=f"bold {CYAN}")
 
         self.update(text)
diff -ruN --ex clorch/tui/widgets/session_list.py /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/tui/widgets/session_list.py
--- clorch/tui/widgets/session_list.py	2026-02-27 08:59:30
+++ /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/tui/widgets/session_list.py	2026-02-27 08:31:28
@@ -22,25 +22,25 @@
     def on_mount(self) -> None:
         text = Text()
         # Col 1: accent (2) + Col 2: num (3) + separator (1) = 6 chars
-        text.append("      ", style="dim")
+        text.append("      ", style=f"{GREY}")
         # Col 3: project name (12)
-        text.append(f"{'NAME':<12s}", style=f"dim {GREY}")
+        text.append(f"{'NAME':<12s}", style=f"bold {GREY}")
         # Col 3b: git branch (10)
-        text.append(f"{'BRANCH':<10s}", style=f"dim {GREY}")
+        text.append(f"{'BRANCH':<10s}", style=f"bold {GREY}")
         # Col 4: status (1 space + 8)
-        text.append(f" {'STATUS':<8s}", style=f"dim {GREY}")
+        text.append(f" {'STATUS':<8s}", style=f"bold {GREY}")
         # Col 4b: stale (5)
-        text.append(f"{'':5s}", style="dim")
+        text.append(f"{'':5s}", style=f"{GREY}")
         # Col 5: tool (1 space + 12)
-        text.append(f" {'TOOL':<12s}", style=f"dim {GREY}")
+        text.append(f" {'TOOL':<12s}", style=f"bold {GREY}")
         # Col 6: tool count (4)
-        text.append(f"{'#T':>4s}", style=f"dim {GREY}")
+        text.append(f"{'#T':>4s}", style=f"bold {GREY}")
         # Col 7: error count (3)
-        text.append(f"{'#E':>3s}", style=f"dim {GREY}")
+        text.append(f"{'#E':>3s}", style=f"bold {GREY}")
         # Col 8: uptime (8)
-        text.append(f"{'UPTIME':>8s}", style=f"dim {GREY}")
+        text.append(f"{'UPTIME':>8s}", style=f"bold {GREY}")
         # Col 9: sparkline (2 space + 10)
-        text.append(f"  {'ACTIVITY':<10s}", style=f"dim {GREY}")
+        text.append(f"  {'ACTIVITY':<10s}", style=f"bold {GREY}")
         self.update(text)
 
 
@@ -61,8 +61,8 @@
 
     def compose(self) -> ComposeResult:
         text = Text()
-        text.append(f"── {self._label} ", style=f"dim {GREY}")
-        text.append("─" * 40, style=f"dim {GREY}")
+        text.append(f"── {self._label} ", style=f"bold {GREY}")
+        text.append("─" * 40, style=f"bold {GREY}")
         yield Static(text, markup=False)
 
 
@@ -186,7 +186,7 @@
         elif agent.status == AgentStatus.ERROR:
             text.append("\u2503 ", style=f"bold {PINK}")
         else:
-            text.append("  ", style="dim")
+            text.append("  ", style=f"{GREY}")
 
         # Col 2: Row number or action letter (3 chars)
         if self._action:
@@ -194,9 +194,9 @@
             text.append(f"[{self._action.letter}]", style=letter_style)
         else:
             display_num = self._row_num if self._row_num <= 9 else 0
-            text.append(f" {display_num} ", style="dim")
+            text.append(f" {display_num} ", style=f"{GREY}")
 
-        text.append(" ", style="dim")
+        text.append(" ", style=f"{GREY}")
 
         # Col 3: Project name (fixed 12 chars)
         project = agent.project_name or agent.session_id[:12]
@@ -216,7 +216,7 @@
                 style=f"bold {CYAN}" if agent.git_dirty_count == 0 else f"bold {YELLOW}",
             )
         else:
-            text.append(" " * self._COL_BRANCH, style="dim")
+            text.append(" " * self._COL_BRANCH, style=f"{GREY}")
 
         # Col 4: Status badge (fixed 8 chars: ">>> WORK", "[!] PERM")
         status_str = f"{symbol} {label:<4s}"
@@ -237,31 +237,31 @@
                     stale_str = f"{int(age_s)}s"
                     text.append(f"{stale_str:<5s}", style=f"bold {YELLOW}")
                 else:
-                    text.append(" " * self._COL_STALE, style="dim")
+                    text.append(" " * self._COL_STALE, style=f"{GREY}")
             except (ValueError, TypeError):
-                text.append(" " * self._COL_STALE, style="dim")
+                text.append(" " * self._COL_STALE, style=f"{GREY}")
         else:
-            text.append(" " * self._COL_STALE, style="dim")
+            text.append(" " * self._COL_STALE, style=f"{GREY}")
 
         # Col 5: Last tool (fixed 12 chars)
         tool = (agent.last_tool or "-")[:self._COL_TOOL]
         text.append(f" {tool:<{self._COL_TOOL}s}", style="white")
 
         # Col 6: Tool count (right-aligned 4 chars)
-        text.append(f"{agent.tool_count:>{self._COL_TCNT}d}", style="dim")
+        text.append(f"{agent.tool_count:>{self._COL_TCNT}d}", style=f"{GREY}")
 
         # Col 7: Error count (right-aligned 3 chars)
         ecnt = f"{agent.error_count:>{self._COL_ECNT}d}"
         if agent.error_count > 0:
             text.append(ecnt, style=f"bold {PINK}")
         else:
-            text.append(ecnt, style="dim")
+            text.append(ecnt, style=f"{GREY}")
 
         # Col 8: Uptime (right-aligned 8 chars)
-        text.append(f"{agent.uptime:>{self._COL_UPTIME}s}", style="dim")
+        text.append(f"{agent.uptime:>{self._COL_UPTIME}s}", style=f"{GREY}")
 
         # Col 9: Sparkline (10 chars)
-        text.append("  ", style="dim")
+        text.append("  ", style=f"{GREY}")
         sparkline = self._render_sparkline(agent.activity_history)
         text.append_text(sparkline)
 
@@ -283,7 +283,7 @@
                 text.append(f"  {msg}", style="dim italic")
 
         if self._action and remaining >= hint_width:
-            text.append("  ", style="dim")
+            text.append("  ", style=f"{GREY}")
             if self._action.actionable:
                 text.append("[y]", style=f"bold {GREEN}")
                 text.append("[n]", style=f"bold {RED}")
@@ -296,16 +296,16 @@
             text.append(f'\n  "{summary}"', style="italic")
             text.append("\n  >>> ", style=f"bold {GREEN}")
             text.append("[y]", style=f"bold reverse {GREEN}")
-            text.append(" APPROVE  ", style="dim")
+            text.append(" APPROVE  ", style=f"{GREY}")
             text.append("[n]", style=f"bold reverse {RED}")
-            text.append(" DENY  ", style="dim")
+            text.append(" DENY  ", style=f"{GREY}")
             text.append("[Esc]", style=f"bold {CYAN}")
-            text.append(" cancel", style="dim")
+            text.append(" cancel", style=f"{GREY}")
 
         # Dim the entire row for unreachable agents (different terminal, not tmux)
         if self._dim:
             for i in range(len(text._spans)):
-                text._spans[i] = text._spans[i]._replace(style=f"dim {GREY}")
+                text._spans[i] = text._spans[i]._replace(style=f"bold {GREY}")
 
         return text
 
@@ -314,7 +314,7 @@
         """Render activity history as a 10-char sparkline."""
         recent = history[-10:] if len(history) >= 10 else history
         if not recent or max(recent) == 0:
-            return Text("\u2581" * 10, style=f"dim {GREY}")
+            return Text("\u2581" * 10, style=f"bold {GREY}")
         max_val = max(recent)
         chars = []
         for v in recent:
diff -ruN --ex clorch/tui/widgets/settings_panel.py /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/tui/widgets/settings_panel.py
--- clorch/tui/widgets/settings_panel.py	2026-02-27 08:59:30
+++ /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/tui/widgets/settings_panel.py	2026-02-27 09:14:28
@@ -60,17 +60,18 @@
         if self._sound_enabled:
             text.append("ON", style=f"bold {GREEN}")
         else:
-            text.append("OFF", style=f"dim {GREY}")
+            text.append("OFF", style=f"{GREY}")
 
-        # Line 2: YOLO — always red background (danger zone)
+        # Line 2: YOLO — auto-approve for tmux agents only
         text.append("\n")
         if self._yolo_enabled:
             text.append("[!] \u26a0 YOLO ARMED \u26a0 ", style=f"bold white on {RED}")
+            text.append(" tmux agents only", style=f"{GREY}")
             if self._rules_count > 0:
-                text.append(f" ({self._rules_count} deny rules)", style=f"dim {GREY}")
+                text.append(f" ({self._rules_count} deny rules)", style=f"{GREY}")
         else:
-            text.append("[!] YOLO OFF ", style=f"bold {GREY} on {DIM}")
-            if self._rules_count > 0:
-                text.append(f" ({self._rules_count} rules)", style=f"dim {GREY}")
+            text.append("[!]", style=f"bold {CYAN}")
+            text.append(" YOLO ", style="white")
+            text.append("OFF", style=f"{GREY}")
 
         self.update(text)
diff -ruN --ex clorch/tui/widgets/status_bar.py /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/tui/widgets/status_bar.py
--- clorch/tui/widgets/status_bar.py	2026-02-27 08:59:30
+++ /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/tui/widgets/status_bar.py	2026-02-27 08:30:55
@@ -26,19 +26,19 @@
         text = Text()
         # Left: branding
         text.append("CLAUDE ORCH", style=f"bold {GREEN}")
-        text.append("  ", style="dim")
+        text.append("  ", style=f"{GREY}")
 
         # Right: counts
-        text.append("W:", style="dim")
+        text.append("W:", style=f"{GREY}")
         text.append(str(summary.working), style=f"bold {GREEN}")
-        text.append("  I:", style="dim")
+        text.append("  I:", style=f"{GREY}")
         text.append(str(summary.idle), style=f"{GREY}")
-        text.append("  [!]:", style="dim")
+        text.append("  [!]:", style=f"{GREY}")
         text.append(str(summary.waiting_permission), style=f"bold {RED}")
-        text.append("  [?]:", style="dim")
+        text.append("  [?]:", style=f"{GREY}")
         text.append(str(summary.waiting_answer), style=f"bold {YELLOW}")
-        text.append("  E:", style="dim")
+        text.append("  E:", style=f"{GREY}")
         text.append(str(summary.error), style=f"bold {PINK}")
-        text.append("    ", style="dim")
+        text.append("    ", style=f"{GREY}")
         text.append(f"{summary.total} agents", style=f"bold {CYAN}")
         self.update(text)
diff -ruN --ex clorch/tui/widgets/telemetry_panel.py /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/tui/widgets/telemetry_panel.py
--- clorch/tui/widgets/telemetry_panel.py	2026-02-27 08:59:30
+++ /Users/azeemansarils/.local/pipx/venvs/clorch/lib/python3.14/site-packages/clorch/tui/widgets/telemetry_panel.py	2026-02-27 08:54:15
@@ -40,7 +40,7 @@
             # Agent name (highlighted if selected)
             name = (agent.project_name or agent.session_id[:_NAME_W])[:_NAME_W]
             is_selected = agent.session_id == selected_id
-            name_style = "bold white" if is_selected else f"dim {GREY}"
+            name_style = "bold white" if is_selected else f"{GREY}"
             text.append(f"{name:<{_NAME_W}s}", style=name_style)
             text.append(" ")
 
@@ -55,10 +55,10 @@
                 bar_color = RED
 
             bar = "\u2588" * filled + "\u2591" * (_GAUGE_W - filled)
-            text.append("[", style=f"dim {GREY}")
+            text.append("[", style=f"{GREY}")
             text.append(bar, style=bar_color)
-            text.append("]", style=f"dim {GREY}")
-            text.append(f" {cc}c", style=f"dim {GREY}")
+            text.append("]", style=f"{GREY}")
+            text.append(f" {cc}c", style=f"{GREY}")
             text.append("  ")
 
             # Sparkline from extended history
@@ -66,7 +66,7 @@
             recent = hist[-_SPARKLINE_W:] if len(hist) >= _SPARKLINE_W else hist
             if not recent or max(recent) == 0:
                 spark = "\u2581" * min(len(recent) or 1, _SPARKLINE_W)
-                text.append(spark, style=f"dim {GREY}")
+                text.append(spark, style=f"{GREY}")
             else:
                 max_val = max(recent)
                 chars = []
